[package]
name = "crossover-gamepad-fixer"
version = "1.1.0"
edition = "2021"
description = "Lightweight Rust binary to toggle the gamepad fix in Wine/CrossOver registry"

[dependencies]
winreg = "0.52"

[build-dependencies]
embed-resource = "2.5"
